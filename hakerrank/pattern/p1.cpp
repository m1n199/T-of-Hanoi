#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{

    int n;
    scanf("%d", &n);
  	// Complete the code to print the pattern.
    int f=1,F=1;
    do {
        for(int i = 0; i < n; i++){
            if(!F && i == n-1 ) break;
            do {
                for(int j = n; j > 0; j--){
                   
                }
            }
        }
    }while(F-- != 0)
    return 0;
}